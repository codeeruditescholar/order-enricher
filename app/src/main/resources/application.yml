spring:
  config:
    import:
      - repository.yml
  threads:
    virtual:
      enabled: true
management:
  endpoints:
    web:
      exposure:
        include:
          - env
          - health
server:
  port: 8080