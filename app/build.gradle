plugins {
    alias(libs.plugins.spring.boot)
}

dependencies {
    implementation(
            libs.bundles.app.project,
            project(':order-enricher-common'),
            project(':order-enricher-repo'),
            project(':order-enricher-adapter'),
            project(':order-enricher-service'),
            project(':order-enricher-api')
    )
}

bootJar {
    archiveFileName = 'app.jar'
    destinationDirectory = file('../build/libs')
}